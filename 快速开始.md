# 小米新闻追踪系统 - 快速开始 🚀

## 一键启动演示（推荐）

### Windows用户

1. **双击运行** `start_demo.bat`
2. 等待程序自动安装和运行
3. 查看演示结果

### Mac/Linux用户

1. 打开终端，进入项目目录
2. 运行命令：
   ```bash
   ./start_demo.sh
   ```
3. 查看演示结果

## 运行效果

演示模式会：
- ✅ 自动创建虚拟环境
- ✅ 自动安装所需依赖
- ✅ 生成24条模拟的小米舆情数据
- ✅ 展示来自小红书、微博、Twitter的内容
- ✅ 显示详细的舆情报告
- ✅ 保存数据到本地数据库

### 示例输出

```
【XIAOHONGSHU】 - 8 条新舆情
----------------------------------------------------------------------

1. 小米14 Ultra拍照效果惊艳
   作者: 数码小达人
   时间: 2025-10-21 14:30
   内容: 用了小米14 Ultra拍照效果惊艳，真的太棒了！👍 特别是拍照，简直让人惊喜...
   互动: 👍2547 💬156 🔄89 👀25483

2. 小米汽车SU7交付量突破新高
   作者: 科技评测君
   时间: 2025-10-21 12:15
   内容: 说实话，小米汽车SU7交付量突破新高确实给力...
   互动: 👍3892 💬243 🔄125 👀38920

...
```

## 查看更多

### 查看数据库统计

```bash
# Windows
python -m src.main --mode stats

# Mac/Linux
python -m src.main --mode stats
```

### 再次运行演示

直接再次运行启动脚本，会生成新的数据

### 查看数据库

数据保存在 `data/xiaomi_news.db`，可以用SQLite工具打开查看

## 下一步：配置真实API

演示模式使用模拟数据。要抓取真实社交媒体数据，需要：

### 1. 配置Twitter API（最简单）

编辑 `.env` 文件：

```env
# Twitter API配置
TWITTER_BEARER_TOKEN=你的Bearer_Token

# 或使用完整OAuth
TWITTER_API_KEY=你的API_Key
TWITTER_API_SECRET=你的API_Secret
TWITTER_ACCESS_TOKEN=你的Access_Token
TWITTER_ACCESS_TOKEN_SECRET=你的Access_Token_Secret
```

**获取Twitter API密钥：**
1. 访问 https://developer.twitter.com/
2. 创建应用
3. 获取Bearer Token或API密钥

### 2. 配置邮件通知（可选）

```env
EMAIL_ENABLED=true
EMAIL_FROM=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_TO=recipient@example.com
```

**Gmail用户注意：**
需要使用"应用专用密码"而不是Gmail密码
- 访问 https://myaccount.google.com/apppasswords
- 生成16位应用专用密码

### 3. 运行真实抓取

```bash
# 运行一次
python -m src.main --mode once

# 持续运行（每小时抓取）
python -m src.main
```

## 常见问题

### Q: 脚本运行失败怎么办？

**检查Python版本：**
```bash
python --version  # 需要3.8或更高
```

如果版本太低，访问 https://www.python.org/downloads/ 下载最新版本

### Q: 演示模式能看到真实数据吗？

演示模式使用AI生成的模拟数据，展示系统功能。要看真实数据需要配置API。

### Q: 不想配置API可以用吗？

可以！演示模式不需要任何API，可以无限次运行查看效果。

### Q: 数据保存在哪里？

- 数据库：`data/xiaomi_news.db`
- 日志：`logs/xiaomi_tracker.log`

### Q: 如何清空数据重新开始？

删除 `data/xiaomi_news.db` 文件即可

### Q: 我想修改搜索关键词

编辑 `.env` 文件：
```env
SEARCH_KEYWORDS=小米14,小米汽车,雷军,澎湃OS
```

## 技术支持

- 查看完整文档：`README.md`
- 查看配置选项：`.env.example`
- 查看代码：`src/` 目录

## 运行模式说明

| 模式 | 命令 | 说明 |
|------|------|------|
| 演示模式 | `python -m src.main --mode demo` | 使用模拟数据 |
| 单次运行 | `python -m src.main --mode once` | 运行一次真实抓取 |
| 持续运行 | `python -m src.main` | 每小时自动抓取 |
| 查看统计 | `python -m src.main --mode stats` | 显示数据统计 |
| 测试报告 | `python -m src.main --mode test` | 生成报告预览 |

## 开始使用

立即运行演示脚本体验系统：

**Windows**: 双击 `start_demo.bat`

**Mac/Linux**: 运行 `./start_demo.sh`

祝使用愉快！🎉
